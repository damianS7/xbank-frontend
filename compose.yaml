name: xbank
services:
  frontend:
    container_name: xbank-frontend
    build:
      context: .
      dockerfile: Dockerfile.frontend
    ports:
      - "8081:80"
    environment:
      - NODE_ENV=production